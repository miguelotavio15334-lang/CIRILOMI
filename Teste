-- HUB SEM FLUENT - EXECUTA 100% NO DELTA - SEM AUTO BUY
repeat task.wait() until game:IsLoaded()
local plr = game.Players.LocalPlayer
local char = plr.Character or plr.CharacterAdded:Wait()
local Root = char:WaitForChild("HumanoidRootPart")

-- CRIA GUI SIMPLES
if game.CoreGui:FindFirstChild("CiriliHub") then game.CoreGui.CiriliHub:Destroy() end
local gui = Instance.new("ScreenGui")
gui.Name = "CiriliHub"
gui.Parent = gethui and gethui() or game.CoreGui
gui.ResetOnSpawn = false

local frame = Instance.new("Frame", gui)
frame.Size = UDim2.fromOffset(250, 180)
frame.Position = UDim2.fromOffset(50, 100)
frame.BackgroundColor3 = Color3.fromRGB(20,20,20)
frame.BorderSizePixel = 0
Instance.new("UICorner", frame).CornerRadius = UDim.new(0,12)
frame.Active = true
frame.Draggable = true

local title = Instance.new("TextLabel", frame)
title.Size = UDim2.fromOffset(250, 40)
title.Text = "🤡 Roube um Palhaço"
title.TextColor3 = Color3.new(1,1,1)
title.BackgroundTransparency = 1
title.TextScaled = true
title.Font = Enum.Font.GothamBold

local base = nil
local farming = false
local kitar = false

local function btn(text, y, callback)
    local b = Instance.new("TextButton", frame)
    b.Size = UDim2.fromOffset(220, 35)
    b.Position = UDim2.fromOffset(15, y)
    b.Text = text
    b.BackgroundColor3 = Color3.fromRGB(40,40,40)
    b.TextColor3 = Color3.new(1,1,1)
    b.Font = Enum.Font.Gotham
    b.TextScaled = true
    Instance.new("UICorner", b)
    b.MouseButton1Click:Connect(callback)
    return b
end

btn("1. SALVAR MINHA BASE", 45, function()
    base = Root.CFrame
    title.Text = "✅ Base Salva!"
    task.wait(1)
    title.Text = "🤡 Roube um Palhaço"
end)

local farmBtn = btn("2. AUTO ROUBAR: OFF", 90, function()
    farming = not farming
    farmBtn.Text = farming and "2. AUTO ROUBAR: ON" or "2. AUTO ROUBAR: OFF"
    farmBtn.BackgroundColor3 = farming and Color3.fromRGB(0,170,0) or Color3.fromRGB(40,40,40)
    if farming and not base then base = Root.CFrame title.Text = "Base auto salva!" end
end)

local kitBtn = btn("KITAR: OFF", 135, function()
    kitar = not kitar
    kitBtn.Text = kitar and "KITAR: ON" or "KITAR: OFF"
end)

-- LOOP DO FARM (SEM AUTO BUY)
task.spawn(function()
    while true do task.wait(0.35)
        if farming and base then
            pcall(function()
                local tool = plr.Character:FindFirstChildOfClass("Tool")
                if tool then
                    Root.CFrame = base + Vector3.new(0,3,0)
                    if kitar then task.wait(1) plr:Kick("Roubado 🤡") end
                else
                    for _,v in pairs(workspace:GetDescendants()) do
                        if v:IsA("ProximityPrompt") and v.Enabled then
                            local txt = (v.ActionText..v.ObjectText):lower()
                            if txt:find("roub") and not txt:find("comprar") and not txt:find("buy") then
                                local hrp = v.Parent:FindFirstChild("HumanoidRootPart") or v.Parent.Parent:FindFirstChild("HumanoidRootPart")
                                if hrp and (hrp.Position - Root.Position).Magnitude < 400 then
                                    Root.CFrame = hrp.CFrame + Vector3.new(0,0,3)
                                    task.wait(0.25)
                                    fireproximityprompt(v)
                                    break
                                end
                            end
                        end
                    end
                end
            end)
        end
    end
end)

print("Cirili Hub carregado - SEM FLUENT")
